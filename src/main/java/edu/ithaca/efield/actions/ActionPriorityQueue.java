package edu.ithaca.efield.actions;

import java.util.PriorityQueue;
//import java.util.concurrent.PriorityBlockingQueue;

public class ActionPriorityQueue extends PriorityQueue<Action> {
//TODO: Maybe in the future if we need to modify something about the priority queue implementation we could do that here and not have to change anything anywhere else
}
